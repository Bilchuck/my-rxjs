const { 
    create, 
    map,
    fromArray,
    filter,
    interval,
    fromEvent,
} = require("./lib/observable");

module.exports = {
    create,
    map,
    fromArray,
    filter,
    interval,
    fromEvent,
};
